// RC command information is from dog
// need to send back to remote PC(with realsense)
// send real action feed into policy to PC for visualization 
struct dog_feedback_info 
{
    float dog_target_coord[2];
    float dog_action[2];
}